# `docs/`

Documentation *about* the project rather than the design system itself. The design system's own
documentation is [`guide/`](../guide/).

| | |
|---|---|
| `adr/` | Architecture Decision Records |

Per KICKOFF §10, three decisions get an ADR because they're the hardest to reverse: **token
architecture**, **site generator**, and **how downstream repos consume the system**.
